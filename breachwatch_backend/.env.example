# Backend Environment Variables Example
# Copy this file to .env and fill in your values

# --- Database (PostgreSQL) ---
# Used by Docker Compose by default (db service name is 'db')
# If running manually, change DB_HOST to localhost or your DB server address
DB_USER=breachwatchuser
DB_PASSWORD=breachwatchpass
DB_HOST=db # Use 'localhost' if running backend manually outside docker-compose
DB_PORT=5432
DB_NAME=breachwatchdb

# Alternatively, provide the full DATABASE_URL (overrides individual components if set)
# DATABASE_URL=postgresql://breachwatchuser:breachwatchpass@db:5432/breachwatchdb

# --- Redis Cache ---
# Used by Docker Compose by default (redis service name is 'redis')
# If running manually, change REDIS_HOST to localhost or your Redis server address
REDIS_HOST=redis # Use 'localhost' if running backend manually outside docker-compose
REDIS_PORT=6379
REDIS_PASSWORD= # Set a password here if your Redis instance requires one
REDIS_DB=0

# --- Application Settings ---
LOG_LEVEL=INFO # DEBUG, INFO, WARNING, ERROR, CRITICAL
ENVIRONMENT=development # development or production
APP_BASE_DIR=/app # Base directory inside the Docker container (usually /app)

# --- Security / JWT ---
# IMPORTANT: Generate a strong, persistent secret key for production!
# Use `openssl rand -hex 32` to generate one.
SECRET_KEY= # Add your secure secret key here (e.g., from openssl rand -hex 32)
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60 # Token expiry time in minutes

# --- Crawler Defaults (Can be overridden by YAML or job settings) ---
# DEFAULT_USER_AGENT=BreachWatchResearchBot/1.0
# REQUEST_TIMEOUT=10 # seconds

# --- Output Location (Relative to APP_BASE_DIR or absolute) ---
# This is overridden by config/default_config.yml by default
# OUTPUT_LOCATIONS_DOWNLOADED_FILES=data/downloaded_files
