# Environment variables for BreachWatch Backend

# --- Database Configuration (PostgreSQL) ---
# Option 1: Use individual components (recommended for clarity)
DB_USER=breachwatchuser
DB_PASSWORD=breachwatchpass
DB_HOST=db # Use 'db' when running with Docker Compose, 'localhost' if running DB natively
DB_PORT=5432
DB_NAME=breachwatchdb

# Option 2: Use DATABASE_URL (overrides individual components if set)
# Format: postgresql://user:password@host:port/database_name
# DATABASE_URL=postgresql://breachwatchuser:breachwatchpass@db:5432/breachwatchdb

# --- Application Settings ---
LOG_LEVEL=INFO # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
APP_BASE_DIR=/app # Base directory inside the container (used for resolving relative paths)

# --- Crawler Settings ---
# These can be overridden by values in config/default_config.yml or API requests
# DEFAULT_USER_AGENT=BreachWatchResearchBot/1.0
# REQUEST_TIMEOUT=10 # seconds

# --- Output Locations ---
# Path where downloaded files will be stored (relative to APP_BASE_DIR or absolute)
# IMPORTANT: Ensure this directory is writable by the application user inside the container/host.
OUTPUT_LOCATIONS_DOWNLOADED_FILES=/app/data/downloaded_files

# --- Authentication Settings (NEW - Required for JWT) ---
# Replace with a strong, randomly generated secret key
# You can generate one using: openssl rand -hex 32
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
